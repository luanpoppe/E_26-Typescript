function multiplicacao(x: number, y: number): number {
  return x * y;
}

let testeMultiplicacao = multiplicacao(5, 3);
console.log(testeMultiplicacao);

function saudacao(nome: string): string {
  return `Olá ${nome}`;
}

let testeSaudacao = saudacao("Maria");
console.log(testeSaudacao);
